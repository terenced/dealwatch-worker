{
  "name": "dealwatch-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "cli": "tsx cli.ts",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "start": "wrangler dev",
    "push-config": "wrangler kv key put --binding KV config --path config.json",
    "push-config:remote": "wrangler kv key put --binding KV config --path config.json --remote",
    "enable:remote": "wrangler kv key put --binding KV active true --remote",
    "disable:remote": "wrangler kv key put --binding KV active false --remote",
    "typegen": "wrangler types",
    "wrangler": "wrangler"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260331.1",
    "@types/node": "^25.5.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "wrangler": "^4.79.0"
  },
  "dependencies": {
    "@discordjs/rest": "^2.6.1",
    "discord-api-types": "^0.38.43",
    "linkedom": "^0.16.11"
  }
}
